/**
 * 회선이 얼마나 빨리 닿는지만 재는 자리.
 *
 * 예전에는 방 목록을 받아오는 시간을 그대로 회선 상태로 삼았다. 그런데 그
 * 요청은 서버에서 데이터베이스를 여러 번 다녀오므로, 그 시간이 값의 대부분을
 * 차지한다. 서버가 느린 날에는 회선이 멀쩡한 사람도 다 같이 빨간불이 됐다 —
 * 사람마다 다른 값이 아니라 "지금 사이트가 얼마나 느린가"를 재고 있었던 것이다.
 *
 * 여기서는 아무것도 하지 않고 곧바로 답한다. 그래서 남는 시간이 곧 그 사람의
 * 회선이다. 게임 안에서 서로에게 가는 핑은 여전히 아니다 — 브라우저는 다른
 * 사람과 직접 주고받을 수 없다.
 */
export default function handler(req, res) {
  res.setHeader('Cache-Control', 'no-store');
  res.status(200).json({ t: Date.now() });
}
